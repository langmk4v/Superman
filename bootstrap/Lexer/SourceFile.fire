namespace firec {
    class SourceFile {
        var path: string;
        var data: string;

        fn open(path: string) -> SourceFile {
            return SourceFile(fs::open(path).read_text());
        }
    }

    class SourceLocation {
        var source: SourceFile ref;
        var position: usize;
    }
}